<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>응급실 호출</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="" crossorigin="anonymous">
</head>
<style>
    #back_ground {
        display: flex;
        justify-content: center; /* 중앙 정렬 */
        align-items: center;
        background-color: lightgray;
        position: relative;
        width: 100%;
        height: 100vh;
        background-image: url('/images/pic.jpg');
        background-position: center;
        background-size: cover;
    }
    #data_input {
        background-color: white;
        padding: 30px;
        width: 30%;
        min-width: 100px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        box-sizing: border-box;
        max-height: 90vh; /* 최대 높이를 90%로 제한 (브라우저 높이보다 커지지 않도록) */
        overflow-y: auto; /* 내용이 넘칠 경우 세로 스크롤 추가 */
    }
    #data_input img {
        display: block;
        margin: 20px auto 20px; /* 중앙 정렬 및 아래쪽 여백 추가 */
        width: 13%; /* 부모 요소의 가로 13% */
        height: auto; /* 비율 유지 */
    }

    /* 작은 화면에서 폼 크기 및 여백 조정 */
    @media (max-width: 768px) {
        #data_input {
            padding: 20px;
            width: 80%; /* 작은 화면에서는 너비 80% */
        }
    }
</style>
<body>
<div id="back_ground">
    <form id="data_input" class="container" action="/login" method="post">
        <img src="/images/siren.png">
        <h3 class="form-title text-center" style="margin: 15px">응급실 호출 웹 서비스</h3>

        {{#globalErrors}}
            <div style="color: red; font-weight: bold;">
                아이디 혹은 비밀번호가 틀립니다.
            </div>
        {{/globalErrors}}
        <div class="mb-3">
            <label class="form-label">아이디</label>
            <input type="text" class="form-control" name="loginId">
        </div>
        <div class="mb-3">
            <label class="form-label">비밀번호</label>
            <input type="password" class="form-control" name="password">
        </div>
        <div>
            <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 20px; margin-bottom: 20px;">로그인</button>
            <a href="/member/signin" class="btn btn-secondary" style="width: 100%">회원 가입</a>
        </div>
    </form>
</div>

{{>layouts/footer}}
</body>
</html>
