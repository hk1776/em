server:
  servlet:
    encoding:
      force-response: true

    session:
      tracking-modes: cookie
      timeout: 1800

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url:
    username:
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

hospital:
  api:
    host: